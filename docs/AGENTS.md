# Contents

- `architecture.md` explains the package seams, runtime split, and safety model.
- `releasing.md` defines the checks-gated immutable release procedure.

# Guidelines

- Document current public behavior and operator procedures. Keep historical rationale and proposed work in `kb/`.
- Keep examples synthetic and product-neutral. Do not include private paths, deployment identities, or provider data.
- Follow `STYLE.md` and preserve exact commands, identifiers, refusal reasons, and security boundaries.
- Update documentation with every public export, runtime requirement, environment variable, or release-process change.
